/// enum for ButtonAnimationBloc
enum WhichTodoBloc {
  mainMenu,
  taskMenu,
}

/// Enum for Button Animation Bloc
enum ActionEnum { reorder, delete, add, action }

// enum for add sum or substract sum
enum AddSubSumTask { substract, add }
